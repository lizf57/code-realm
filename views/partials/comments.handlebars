<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="comment-info">
            {{user.username}} on {{createdAt}}
        </div>
        <div class="comment-text">
            {{comment_content}}
        </div>
    </section>
    {{/each}}
</div>